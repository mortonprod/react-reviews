import ReviewTemplate from "./reviewTemplate";
/**
 * We can add more variables and member functions if needed.
 */
export default class ReviewDefault extends ReviewTemplate {
    constructor(obj: review) {
        super(obj);
    }
}
